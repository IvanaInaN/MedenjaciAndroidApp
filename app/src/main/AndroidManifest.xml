<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name="com.example.pkiProject.products.ProductDetails"></activity>
        <activity android:name="com.example.pkiProject.basket.BasketActivity" />
        <activity android:name="com.example.pkiProject.user.UserActivity" />
        <activity android:name="com.example.pkiProject.products.ProductsActivities" />
        <activity android:name="com.example.pkiProject.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>